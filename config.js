const JWT_USER_PASSWORD = 'your_jwt_secret_key';
const JWT_ADMIN_PASSWORD = 'your_admin_jwt_secret_key';

module.exports = {
    JWT_USER_PASSWORD,  // Exporting JWT_USER_PASSWORD as-is
    JWT_ADMIN_PASSWORD  // Exporting JWT_ADMIN_PASSWORD as-is
};
